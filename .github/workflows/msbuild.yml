name: OSMesaWin

on:
  push:
    branches: [ "main" ]
  # pull_request:
  #   branches: [ "main" ]

env:
  MESA_VERSION: 24.3.3

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    
    - uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: ${{ runner.arch }}
        vsversion: 2022
        
    - name: download mesa source
      run: |
        Invoke-WebRequest https://archive.mesa3d.org/mesa-24.3.3.tar.xz -OutFile mesa-24.3.3.tar.xz        
        dir
        
    - name: unzip mesa source
      run: |
        7z x mesa-24.3.3.tar.xz -omesa
        dir mesa

    - name: unpack mesa source
      run: |
        7z x mesa/mesa-24.3.3.tar -omesa_src
        dir mesa_src
        move mesa_src/mesa-24.3.3 .
        
    - name: prereqs
      run: choco install winflexbison pkgconfiglite ninja

      
    - name: install meson
      run: python3 -m pip install meson packaging mako PyYAML
      
    - name: build
      run: |
        cd mesa-24.3.3
        meson setup builddir -Dosmesa=true -Dgallium-drivers=softpipe -Dvulkan-drivers="[]" -Dprefix="$PWD/builddir/install" --backend=vs
        meson compile -C builddir
        meson install -C builddir
            
    # - name: Add MSBuild to PATH
    #   uses: microsoft/setup-msbuild@v1.0.2

    # - name: Restore NuGet packages
    #   working-directory: ${{env.GITHUB_WORKSPACE}}
    #   run: nuget restore ${{env.SOLUTION_FILE_PATH}}

    # - name: Build
    #   working-directory: ${{env.GITHUB_WORKSPACE}}
    #   # Add additional options to the MSBuild command line here (like platform or verbosity level).
    #   # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
    #   run: msbuild /m /p:Configuration=${{env.BUILD_CONFIGURATION}} ${{env.SOLUTION_FILE_PATH}}

    # - name: get_source
